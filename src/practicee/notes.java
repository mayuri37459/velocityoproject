package practicee;

import java.util.List;

import org.openqa.selenium.WebElement;

public class notes {
	
/*locators(byname, byid, byclassname,bycssselector,partaillinktext, linktext), Byxpath(byattribute, bytext, bycontains, byindex, absolute xpath, relative xpath)
byfollowing-sibling, precceding-sibling, child, parent
backward, forward. refresh, navigate, get, position,  maximize, minimize, getcurrentUrl, gettitle, isenabeled, isdisplayed,isselected, draganddrop, screenshot, dropdwon,
scroll up scrolldown, scrollbyelement, popups(alert, file upload download, authentication, child popup, ), seletbyvalue, selectbyindex, selectbyvisibletext, Listbox(dropdown),
iframe(frame, parentcontent, defaultcontent), waits(implict, explicit, fluent), findelement, findelements, exceltable(getcell, getrow, workbookfactory, getStringCellValue,getnumericCellValue  ),
dynamictable(tr, td, List <WebElement>, gettext)  */

}
